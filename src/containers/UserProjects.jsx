import React, { useEffect } from 'react';
// import { useDispatch, useSelector } from 'react-redux';

import UserInfo from '../components/UserProjectList';
// import action/ selector after pull from Jonas' merge
// const selectedUserProjects = useSelector(getUserProjects);

export default function UserProjects() {
  useEffect(() => {});

  return (
    <div>
      {/* <UserInfo selectedUserProjects={selectedUserProjects} /> */}
    </div>
  );
}
