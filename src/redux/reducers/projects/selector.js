export const getAllProjects = (state) => state.projects.allProjects;

export const getProject = (state) => state.projects.selectedProject;

export const postNewProject = (state) => state.projects.newProject;
